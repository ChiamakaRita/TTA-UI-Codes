export interface MarketOutlookPayload {
  title: string;
  content: string;
  active: boolean;
  userId: number;
}

export interface MarketOutlookData {
  id: number;
  title: string;
  content: string;
  created: string;
  active: boolean;
}
