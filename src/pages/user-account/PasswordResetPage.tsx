import { observer } from "mobx-react-lite";
import ResetPassword from "../../components/user-account/reset-password/ResetPassword";

export default observer(function PasswordResetPage() {
  return (
    <>
      <ResetPassword />
    </>
  );
});
