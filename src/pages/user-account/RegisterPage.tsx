import { observer } from "mobx-react-lite";
import Register from "../../components/user-account/register/Register";

export default observer(function RegisterPage() {
  return (
    <>
      <Register />
    </>
  );
});
