import { observer } from "mobx-react-lite";
import Login from "../../components/user-account/login/Login";

export default observer(function LoginPage() {
  return (
    <>
      <Login />
    </>
  );
});
